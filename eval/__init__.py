"""memovox evaluation harness (spec §10).

A small, pure-stdlib measuring stick for the knowledge + trust layers, run over
the hand-labeled golden corpus in ``eval/golden/``. See ``eval.harness``.
"""
